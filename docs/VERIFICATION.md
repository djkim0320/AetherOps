# Verification status

This file records observed evidence without treating protocol fixtures, executable diagnostics, or packaging smoke tests as release-quality engineering results. Unless a result is explicitly identified as current, it describes an earlier local source snapshot rather than one final release candidate. The current developer executable is identified below, but every portable/installer artifact and package hash remains historical and must be regenerated. Release packaging also intentionally fails until the private stable runtime feed URL, key id, and Ed25519 public key are supplied as the three `AETHEROPS_RUNTIME_*` build inputs.

## Current source status after runtime/time/live-contract hardening

The current source implements v2 `runtime-trust-diagnostic` with an explicit `build_mode`. Release packaging and release-ledger preparation require `release`, configured embedded trust, and exact digests. `AETHEROPS_DEV`, data-root/trust overrides, Node variables, proxy variables, working-directory manifests, and source-tree sidecars cannot replace the executable-adjacent trust boundary. Evaluation isolation is available only through an explicit owned `--data-root`; normal startup always uses `%LOCALAPPDATA%\AetherOps\v2`.

The runtime manager now surrounds activation with the durable `activating → pointer_swapped → active` journal and reconciles it before paths are exposed. It commits forward a completely verified swapped candidate, rolls back and quarantines an interrupted prepared candidate, and blocks startup when the pointer, journal, and candidate cannot be reconciled. Three injected crash boundaries are each tested across two reopens.

Knowledge assertion times are canonicalized to fixed-width UTC nanoseconds for new projections, curation, patch adoption, inference, keys, and shadow copies. Existing noncanonical rows are read semantically. Proof intersection and retention use parsed `time.Time` values, inclusive equal boundaries are valid, and invalid/reversed active-head intervals fail closed before snapshot use without changing migrations 1–14.

`live_end_to_end` journal, details, and producer contracts are v2. Runner session A may stop for offline 12/12 verification; observation session B must be a new protected session for the same ProductBuild/project/data root with `started_at >= evaluation.verified_at`. Runner and observation endpoint hashes plus the observation descriptor hash are independently bound; v1 evidence is rejected.

Direct `tools/package.ps1` invocation now validates the current process's three packaging inputs independently, runs that diagnostic against the exact `build/aetherops.exe`, and requires field-for-field equality before any portable staging begins. Missing, partial, stale, development-only, malformed, or differently configured embedded trust therefore stops packaging. Portable launch smoke removes all three trust environment variables before starting the extracted executable, so inherited development configuration cannot hide an omitted build-time trust root.

Targeted Go tests, strict PowerShell parser/mismatch tests, PowerShell syntax parsing, and the dependency policy passed locally. An ephemeral `windowsgui` test executable proved that malicious development overrides do not change a correctly embedded diagnostic and that an unconfigured executable remains rejected even when all three environment values are present. Direct packaging now also reproduces both checked-in SBOMs before staging, enforces the complete third-party notice/no-first-party-license policy, deletes only the exact stale expected installer and hash manifest, and writes `SHA256SUMS.txt` only after both the portable ZIP and installer exist. This is source-level and local developer-build evidence only: no portable ZIP or installer was regenerated, `dist/SHA256SUMS.txt` was not reissued, and no existing distribution artifact is a package of the current source.

## Local evidence by source snapshot on Windows 11 x64

- Go 1.26.5 module verification, complete unit/integration suite, `go vet`, and a trimmed `aetherops.exe` build.
- Repeated shuffled state, dispatcher, scheduler, CAS, API, browser-policy, Codex-protocol, and recovery tests.
- Actual Evergreen WebView2 Gate 0: isolated shell/internet environments and profiles, shell CDP absent, internet CDP live on random IPv4 loopback, guarded download directory, no internet bridge/host objects, DevTools UI disabled, password/autofill disabled, and permission denial installed.
- Actual Windows Credential Manager temporary credential round trip.
- Frontend TypeScript build, npm audit with zero known vulnerabilities, and Playwright visual inspection at desktop width.
- Dependency-boundary policy, CycloneDX 1.5 SBOM, and SPDX 2.3 SBOM generation.

### Ontology/knowledge-graph source snapshot

The current 2026-08-09 integrated source snapshot passed `go test ./...`, `go vet ./...`, the dependency policy, the frontend TypeScript/Vite build and five production-adapter tests, and the real Oxigraph sidecar suite through `tools/dev.ps1 test`. The sidecar suite loaded SHA-256 authenticated canonical N-Quads and passed all ten cases, including immutable-snapshot cache reuse, project/generation isolation, bad-hash rejection, row-limit atomicity, aggregate/subquery/property-path/ASK/CONSTRUCT/DESCRIBE execution, and `UPDATE`/`SERVICE` rejection.

The implementation now includes migrations 6–14, immutable shadow generations, append-only evidence-backed curation, persisted and cycle-checked RDFS type proof chains, partial RDFS/OWL schema projection, functional-property conflict checks, canonical schema-bearing and typed-literal N-Quads, read-only project-scoped SPARQL, deterministic engineering observations, successful-run recovery, pinned-document extraction/review, immutable run research pins, and fail-closed `hybrid_graph_v1` retrieval. Migration 11 preserves interrupted attempt audit identity across an explicit retry; migration 12 atomically rejects a legacy invalid run status and installs database guards for exactly the 13 public run states; migration 13 binds user-authored curation memos to a server-created project document, deterministic chunk, CAS blob, UTF-8 byte span, and span hash; migration 14 adds a server-authored append-only locator and unique run/document/chunk/role receipt boundary for legacy successful-report backfill. Legacy reports that predate `KnowledgePatch` use only adopted evidence CAS byte spans and the fixed isolated Terra/high extractor followed by Sol/xhigh reviewer. Model thread/turn and input/output/patch hashes survive a crash; ambiguous requests require an explicit user retry, while completed receipts are cloned into a clean shadow without another model turn. Before a curation event reaches the append-only ledger, the complete pending ledger plus proposed edit is replayed into a disposable generation and must pass ontology, provenance, evidence, inference-proof, duplicate-assertion, and canonical N-Quads validation; crash-left validation generations are removed only by their exact contract. Per-project graph edits, ontology activation, and rebuilds share one mutation lease. Every active generation requires one CAS-backed canonical N-Quads snapshot, including an ontology-only graph with zero instance assertions. New projects publish and read back the schema snapshot before a single SQLite transaction exposes the project and ready head; setup mode returns HTTP 503 instead of creating an unverifiable head. Startup uses a bounded multicore recovery pass to replace only legacy snapshotless zero-instance heads with verified shadow generations, while corrupt or non-empty heads fail closed. Run-pinned generation tests prove an active-head swap cannot change in-flight knowledge reads; a missing pinned CAS snapshot terminalizes the run before any model turn and marks only the matching active head failed. Successful-run adoption now applies the same project-scoped unique exact-identifier/alias identity policy as pinned extraction, remaps assertion subjects, objects, and qualifiers, and unions evidence for semantic duplicates; ambiguous aliases, type conflicts, and uncurated new aliases fail closed. JSON-LD and RDF/XML imports now reject unsupported or ambiguous keyword, literal, attribute, resource, and language/datatype combinations instead of silently changing their meaning. Assertion-edit tests cover atomic typed-literal/SI/qualifier/time/evidence replacement and reject unsupported units, NaN, and incomplete literal schemas without partial writes. Security regression tests cover proof cycles and semantic mismatch, ontology-key collision, cross-project composite foreign keys, REST/MCP project binding, external SPARQL datasets, remote RDF imports/contexts, JSON-LD scoped-context injection, extractor prompt injection, non-executable HTML payload handling, raw curation evidence injection, cross-project memo binding, and memo span/hash tampering.

Project memory now has an explicit active/shadow pointer and monotonic `memory_revision`. Reindexing uses the real embedding client, writes only a shadow index, verifies complete chunk coverage, dimensions, finite float32 values, and hashes, then atomically retires the former active index. Startup activates only a complete checksum-valid interrupted build without repeating an embedding request and fails incomplete/corrupt candidates while preserving the prior active head. Immediate SQLite transactions make research creation and reindex creation mutually exclusive in both directions, including scheduled runs. Pin, index, forget, delete, and graph-adoption mutations are also rejected while the project has active/recoverable research or a shadow build; pinning checks before the external embedding call and again at commit. Settings exposes status, revision, error, and the real reindex action.

The versioned Korean/English knowledge gold gate directly exercises production identity normalization/`normalizePinnedPatch` and the real CAS/`mapTextEvidence` path. Twenty identity decisions plus three normalization cases produced 100.00% automatic-merge precision (8 true positives, 0 false positives), while twenty UTF-8 byte-span cases produced 100.00% evidence accuracy. The span set includes Korean, English, mixed text, combining characters, emoji, trimming, overlap-window mapping, and an expected fail-closed CRLF-crossing case. The fixed release minima remain 99% and 98%, respectively.

The current candidate's opt-in 50,000-chunk exact retrieval gate ran on the real SQLite WAL/FULL store with 1,536-dimension float32 vectors and all 16 logical processors. Observed p95 values were 1,571.308 ms for `hybrid_v1`, 1,753.481 ms for `hybrid_graph_v1` (+11.594%, below the 25% ceiling), and 3.182 ms for graph expansion (below the 75 ms ceiling). The final context contained 12 results, including 8 graph-derived and 4 graph-only results, preserved whole disputed-evidence pairs, and kept results per source artifact at 2. The candidate-bound receipt is `build/evidence-rag-50000-4376ba95.json`, SHA-256 `587b2e55bc4b8896ad18281f7f95c2ed8113a7d642eac6776a8f166689fabcf0`; its typed sibling details preserve the complete artifact and command-output hashes.

Scheduler durability tests used real SQLite close/reopen and child-process `os.Exit(93)` crash boundaries. They passed the duplicated fall-DST wall time, pending approval, and claim-commit/cursor-update cases with zero duplicate `(schedule_id, scheduled_for)` firings. Catch-up now advances in bounded pages beyond 10,000 expired occurrences, records each expired occurrence, preserves its cursor durably across restart, and queues only the latest occurrence inside the 24-hour window; an optimistic cursor update prevents a stale scheduler tick from moving `next_run_at` backward.

Startup lifecycle tests now reconcile the authoritative SQLite blob registry with CAS before any writer starts. A per-user-session named mutex is acquired before storage opens, so a second process cannot race migration, recovery, or CAS reconciliation; it only activates the primary window. Project and memory deletion no longer unlink CAS online after their database commit, eliminating a same-hash re-adoption race; they report `cas_cleanup_pending`, and the next start removes only registry-unreachable CAS objects and abandoned temporary files with a bounded multicore worker pool. A regression test proves that a same-hash object re-adopted before restart survives reconciliation. Stage input/output hashes are included in project-deletion and durable reachability calculations so shared audit objects cannot be orphaned. Unexpected CAS paths, links, or malformed object names still stop reconciliation without deletion.

Restart recovery now closes incomplete read-only attempts as `interrupted` without retrying a turn. Only explicit user resume archives the incomplete audit row under a distinct physical ordinal and creates a fresh attempt with the same logical ordinal. A completed side-effect attempt is reused without replay only when its published artifact and immutable execution receipt match the run, thread, turn, input/output hashes, product build, and fixed stage model/reasoning/tier; an incomplete side effect remains `uncertain`. Durable project FIFO prevents later queued runs from bypassing older interrupted, uncertain, approval-waiting, or otherwise nonterminal work after restart, and discard wakes the blocked queue.

The managed runtime updater now durably quarantines a rejected release id, restores pending/active state from disk in a fresh process, and retains an activation-failure warning across restart. Candidate App Server probing uses the dedicated AetherOps `CODEX_HOME`, completes the stable initialize handshake, and requires `model/list` to expose the exact `gpt-5.6-sol/xhigh` and `gpt-5.6-terra/high` pairs without a fallback. Candidate browser probing requires both `list_pages` and `take_snapshot`. Local tests authenticate Oxigraph with the same SRI/signature and sidecar compatibility probes as the other managed components. `cmd/productionfeedevidence` is now the typed trusted producer for `production_update_feed`: it binds the exact prepared-ledger predecessor and candidate, authenticates the candidate's embedded feed trust, permits only public DNS/system-TLS HTTPS port 443, records signed envelope/payload/certificate hashes, performs the real eight-component download plus SHA-256/attestation/npm SRI checks and live App Server/browser probes, and reopens the Manager before invoking `Updater.ActivateOnStartup` for pending-to-active readback. The command has no injectable network or probe path; unit fixtures verify rejection only. The real private feed, production Ed25519 public trust input, running WebView2 CDP endpoint, and successful live receipt remain external and are not claimed until that command actually succeeds.

The versioned evaluation dataset remains exactly 12 cases (six general, six engineering) with strict UTF-8/JSON validation and SHA-256 `18b1c1814efe1f75e39bdf0d80b2db58edade61d35fbc790f44c90796ed01678`. The exact product now exposes a protected `release-eval-session` descriptor, and `cmd/releaseevalrunner` submits those prompts through the live loopback product API. Its fsynced journal records `SUBMITTING` before every POST, never retries an ambiguous submission, resumes a known run using GET only, and observes but never grants approvals. The token is current-user-only, remains out of argv/log/journal/receipt, and is deleted with the descriptor when the product stops. `cmd/releaseeval -mode verify-runner` then opens SQLite/CAS offline, authenticates the packaged Node/Oxigraph runtime, and is the only producer allowed to emit `live_quality_12` evidence. Both it and `cmd/packagedblackbox` require the current prepared-ledger path, validate the full chain and exact candidate before observation, reauthenticate the ledger before evidence emission, and bind its SHA-256 as the `prepared-ledger` subject; a receipt cannot be replayed against another ledger for the same candidate. Typed release admission requires the runner provenance and dataset/runner hashes, exactly 12 passed cases, exact model-stage receipts, 100% citation and knowledge evidence integrity, zero unsupported assertions and critical errors, every quality axis at least 3, average at least 4, CAS/memory/graph materialization integrity, and real RDF snapshot readback. Manual manifests remain diagnostic-only. Unit fixtures prove rejection mechanics only and do **not** count as the required 12/12 live research result.

`cmd/livee2eevidence` now owns a separate two-phase `live_end_to_end` proof. Its live phase binds the prepared ledger, exact candidate, real evaluation receipts, protected product session, DevTools MCP `list_pages`/`take_snapshot`, one successful research run, project-scoped SPARQL, and an evidence-backed `pin_entity` curation into a fsynced hash-chain journal. Every POST is preceded by a durable submitting marker and an ambiguous outcome is never replayed. Its offline phase requires the product to be stopped, reauthenticates SQLite/CAS, fixed stage model receipts, internal-MCP evidence origin, the pinned XFOIL execution receipt, canonical RDF snapshot, SPARQL generation, curation memo, and exact subject set before it can emit release evidence. Fixtures exercise rejection only; no live receipt has been emitted without the required credentials and services.

SU2 preflight now requires AVX2, FMA, BMI1, BMI2, and the XMM/YMM OS state established by CPUID OSXSAVE plus XGETBV. The current host reported all five and passed. Table-driven tests reject each missing capability independently, but evidence from a genuinely incompatible Windows machine is still required below.

Actual Evergreen WebView2 Gate 0 passed again with runtime version `151.0.4129.59`: separate shell/internet profiles, no shell CDP configuration, a live random internet CDP port, isolated downloads, no native bridge/host objects, disabled DevTools UI/password/autofill, and the permission-deny handler. The then-installed `windowsgui` executable rendered the dark native frame and Knowledge screen with 28 ontology nodes and 15 edges. Executing the default read-only `SELECT ?subject ?predicate ?object WHERE { ?subject ?predicate ?object } LIMIT 100` in the real UI returned non-empty rows from Oxigraph, including the RDFS range triple for `urn:aetherops:core:uses`. Read-only inspection of the stopped application's SQLite database found two active ready heads, each with exactly one snapshot and 111 triples, and zero active heads without a snapshot. Closing the title bar hid the window while keeping the exact process alive; launching the executable again exited the activation process successfully and restored the same window. The verified process was then stopped, leaving no AetherOps process running.

A final current-source Gate 0 rerun passed with Evergreen WebView2 runtime version `151.0.4129.72` and all ten operational checks set to `passed: true`: DevTools MCP control, three real WebView2 controllers, Korean IME DOM readback, Per-Monitor v2 DPI, tray restoration, profile reopen, emergency stop, manual-resume re-observation, private-network blocking, and DNS-rebinding blocking. The final GUI executable returned exit code 0 from the same harness; a console-subsystem diagnostic build of the identical source emitted the typed JSON shown by the verification run. This is local browser-isolation evidence only; it was not run from a regenerated package and does not establish portable, installer, clean-VM, or release status.

The current local developer build is `build/aetherops.exe`, version `0.1.0-alpha.1`, 16,607,232 bytes, SHA-256 `0cd2a9a7d130632f08dcd5191797acf37e2272528df2748a1c28db31b46eb1b1`. It is deliberately a `development` build with no embedded private feed trust, so it is not assigned a release candidate id and cannot prepare a release ledger or package. The former `b88efea0…` candidate, every `4376ba95` ledger/evidence chain, and all prior portable/installer hashes are historical and ineligible. Actual credentials, production feed, incompatible SU2 hardware, two fresh clean VMs, live 12/12, and full live-end-to-end gates remain external release requirements.

The packaged black-box campaign force-terminated the exact EXE twice and passed SQLite integrity, `interrupted`/`uncertain` recovery, external thread/turn preservation, zero duplicate recovery events and attempts, reachable CAS readback, orphan/temp cleanup, incomplete generation cleanup, idempotent second startup, WAL checkpointing, runtime content-hash rejection, sidecar prelaunch identity rejection, and post-campaign candidate reauthentication. Its receipt is `build/evidence-packaged-blackbox-4376ba95.json`, SHA-256 `f761d90ee0ef0cfe422b6458dfe5065c6cc73ce891a1f38e5713354a7560d983`.

Computer-use inspection of this exact EXE used an isolated development data root. The native dark frame and themed scrollbars rendered; project and conversation creation completed; the model popover selected Terra, changed reasoning from `xhigh` to `medium`, and selected `fast`; the Knowledge lazy-loaded editor reported a ready empty generation with Instance/Ontology modes, accessible graph listing, evidence-backed edit/RDF/pinned-material surfaces; and the default read-only SPARQL query completed against the real Oxigraph sidecar in 12 ms. Its redacted trust diagnostic remains `configured:false`, so it is intentionally ineligible for production packaging. This developer executable is not a portable, installer, or release artifact.

The real sealed engineering adapter E2E passed again without a fake solver result:

- OpenVSP/VSPAERO receipt `e6650961557a56b40b7ced0e53f85e5c4a8a36b2bcd8430bf26c849055705057`
- OpenVSP model-modification receipt `435a6113f567f5d8e71e984bcf48c311c4fa7f1fbfd80481e1541bd57ca67bbc`
- Gmsh receipt `cf99764f2dde508de8e87526635ddbc9623cb66bb75f4013969f1114973b6d54`
- XFOIL receipt `3fb9b140234f3cd8f12375b6c94700c97407a20588253eb9c6cc948e8ff8ba73`
- SU2 receipt `3be48119e2fe3611f951ea24d4a35d27d9f0fc288960726426a6c3a05584cea9`; 121 iterations, final RMS density `-8.022062702`, CL `0.3518916712`, CD `0.0154279975`

### Historical eight-component packages (stale after current source changes)

The superseded source snapshot represented by the hashes below was packaged after its then-current checks. Package input validation authenticated the eight managed runtime components, first-party knowledge-sidecar scripts, 31 packaged source/license receipts, notices, and both SBOM formats. The bundled initial runtime used a closed, authenticated `componentRoots` mapping under `runtime/b/<code>` while update candidates and user-data runtimes retained the normal generation layout. Runtime metadata and full tree SHA-256 verification were unchanged. The packaging gate rejected any staged relative path longer than 160 characters. The portable archive contained 3,377 entries and expanded to 1,290,252,144 bytes; its observed longest entry was 159 characters. The executable, Oxigraph WASM, sidecar scripts, receipts, source/license files, notices, and both SBOMs were found. That embedded-UI `build/aetherops.exe` was 15,921,152 bytes with SHA-256 `0d9df3b240bd00963f3df7908e25efa4d014357847a7e437969efb7c3dd60692`. The runtime manifest SHA-256 was `3ba3fa62db8bebd0f2c531808165baad1af5f8e4c3252fe8b037f702ea6e5317`; the first-party sidecar SHA-256 was `d7ec01bab9a5fb3cd29c4a67f77b25f2dbf87ef61ef223196484c4aa53b29963`.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `AetherOps-0.1.0-alpha.1-windows-x64-portable.zip` | 688,632,577 | `4589ae353b98eef978318a03c7d79b85f8e56afcc5bb1d6913e395fedb239f30` |
| `AetherOps-0.1.0-alpha.1-windows-x64-setup.exe` | 580,216,310 | `6f1a093caf7b4060bd956a86fb072b2722349c817298059af30445f60740b769` |

The historical `dist/SHA256SUMS.txt` matched both files. `build/aetherops.exe version` returned `AetherOps 0.1.0-alpha.1`. Windows PowerShell 5.1 successfully expanded that exact portable ZIP under the long workspace destination `build/package-smoke-schema-snapshot-20260809-long-root` in 37.7 seconds. The extraction contained 3,311 files; the longest relative path was 159 characters; all eight roots named by `runtime/active.json` existed; and the extracted executable exactly matched the then-current build SHA-256. A hidden launch with `AETHEROPS_DEV=1` and an isolated `AETHEROPS_DATA_DIR` remained alive, created SQLite plus both WebView2 profile roots, and left zero package-tree processes after forced stop. The temporary extraction and data roots were then moved to the Windows Recycle Bin. This closed the observed same-host legacy MAX_PATH failure for that historical package only; a regenerated current package and clean-VM extractor coverage remain required.

Inno Setup 7.0.2 compiled the historical installer from the exact compact runtime staged for its portable archive; static policy rejected a direct long-layout runtime source. The installer updated the existing AetherOps product location at `D:\AI\AetherOps\AetherOps`. Its executable matched that build's SHA-256 and the compact runtime was present. The installed build supplied the real Knowledge/SPARQL and tray-restore observations above. That pre-existing product installation was intentionally not removed, and the process was stopped after verification. The uninstall code and reviewed deletion targets are unchanged from the earlier locally verified default-preserve removal test, but the current source still requires package regeneration and clean-VM removal verification. The initial private-alpha installer remains intentionally unsigned. These are historical local artifacts, not current package or release evidence.

### Previously verified seven-component managed runtime (superseded)

The evidence below predates the required Oxigraph 0.5.9 component and is retained only as historical engineering-tool evidence. The runtime contract now requires eight components plus the first-party knowledge-sidecar scripts. The old package hashes and package smoke results are invalid for the current source tree and must not be used as release evidence.

On 2026-08-08, `tools/runtime-bundle.ps1` built, sealed, activated, and then re-verified this complete set without using `PATH` fallbacks:

| Component | Version / variant | Verified acquisition SHA-256 | Hash authority |
| --- | --- | --- | --- |
| Node.js | 24.19.0 win-x64 | Recorded in `.runtime/verification-receipt.json` from the official Node SHASUMS file | Official Node digest plus valid Authenticode |
| Codex CLI | 0.146.1 win32-x64 | npm SRI and lock receipt in `.runtime/verification-receipt.json` | npm signature and attestation verified |
| Chrome DevTools MCP | 1.6.0 | npm SRI and lock receipt in `.runtime/verification-receipt.json` | npm signature and attestation verified |
| OpenVSP / VSPAERO | 3.50.4 win64, Python 3.13 | `bd8cf75fe19c15eb13f941b07c315e92e04584d6ee94308f7d466adf67f0bc7c` | Reviewed vendored acquisition hash; upstream publishes no detached digest |
| Gmsh | 4.14.1 Windows64 | `b3464b7e170f39639bc74b47334e7eab5cb44fa9317d16ff0c55b42bb04a1b80` | Reviewed vendored acquisition hash; upstream publishes no detached digest |
| XFOIL | 6.99 official Windows binary | `e13e8fe5cc38d8ac2626e9d3b17643bdcfaa63791619f042afdaa7cd103bcb08` | Reviewed vendored acquisition hash; upstream publishes no detached digest |
| SU2 | 8.5.0 win64-omp | `4466fe21aedb5e0bad57afd45f829acbdec6ec79fe8c3f8954ddea06a4b4bc11` | Official GitHub release digest |

The SU2 outer archive was required to contain exactly one `win64-omp.zip`. Its observed nested SHA-256 is `48e8a1c1cd5db8f545612c6e86cc2a386487a02b1c35b6ac4de491b0c67e1e59`. Safe extraction rejected absolute paths, traversal, Windows device names, case-insensitive duplicates, symbolic links, excessive entry counts, expansion sizes, and compression ratios. PE inspection accepted x64 for OpenVSP, VSPAERO, Gmsh, and SU2. XFOIL was explicitly accepted only as the upstream x86 executable and is labeled `x86-on-windows-x64`.

Direct process smoke observations:

- `vspscript.exe -help` reported Vehicle Sketch Pad 3.50.4 and exited 0.
- `gmsh.exe -version` reported 4.14.1 and the process exited successfully.
- XFOIL accepted `QUIT`, reported version 6.99, and exited 0.
- `SU2_CFD.exe --help` reported SU2 8.5.0 Harrier and exposed its OpenMP `--threads` option, exiting 0.
- `vspaero.exe -help` treats `-help` as an input basename and exited 1 because `-help.vspaero` did not exist. This proves process startup and input rejection only, not a solver result.
- `SU2_SOL.exe --help` requires a configuration file and exited 1. This proves process startup and input rejection only, not a solver result.

The bundle contains the full upstream license texts and corresponding-source acquisitions under `.runtime/licenses` and `.runtime/sources`. `.runtime/source-acquisition.json` records every URL, SHA-256, source-tree placement, OpenVSP/SU2 commit pin, and SU2 submodule mapping.

The acquisition manifest contained 35 records and mapped 31 packaged license/source files. Each mapped file passed its recorded size and SHA-256 receipt; the manifest itself matched the then-current verification-receipt SHA-256 `f5594bac7285f4e6f27ebf174786d4b2dd3df3d0967c453b8574afba8213061f`. This historical `tools/package.ps1 -VerifyOnly` result covered only seven runtime components and is superseded.

### Portable package (superseded)

The source-inclusive portable ZIP was built and inspected successfully:

- File: `dist/AetherOps-0.1.0-alpha.1-windows-x64-portable.zip`
- Size: 685,066,824 bytes
- SHA-256: `f7ca1bfcb7f619a70949bd32903806148956e0c30ba9ecf7f89b0a86e47e2204`
- ZIP entries: 3,366
- Uncompressed bytes: 1,279,618,880
- Required runtime executables, active/verification receipts, all four license sets, top-level source archives, SBOMs, and third-party notice were present.

The staged portable executable was also launched with an isolated data directory. It remained alive, initialized SQLite, and started the bundled Codex App Server from its own `runtime` tree.

### Installer package (superseded)

The repository-local, portable installation of Inno Setup 7.0.2 x64 was verified before use. The upstream installer was 17,020,192 bytes with SHA-256 `5ad54ca3def786f8f4212552e54cc6d8d61329e2d24a1cfee0571d42c2684ff1`, had a valid Authenticode signature from `Pyrsys B.V.`, and installed `ISCC.exe` with a valid signature. The compiler reported engine version 7.0.2.

- File: `dist/AetherOps-0.1.0-alpha.1-windows-x64-setup.exe`
- Size: 578,642,001 bytes
- SHA-256: `96bf3d202bafb7ec839102a21d6856d21cca168411a264736e7fb0cdfdbdb743`
- Signature: intentionally unsigned for the initial private alpha distribution

The installer was run silently into an isolated directory on this Windows 11 host. Fourteen required application/runtime/license/source/SBOM paths were present. The installed executable remained alive, initialized its isolated SQLite store, and started the bundled Codex App Server from the installed runtime. Silent uninstall then removed the program while preserving the isolated user-data directory, as required.

The superseded `dist/SHA256SUMS.txt` contained and matched both artifacts from that older package snapshot.

### Real engineering-adapter E2E

`TestRealBundledEngineeringAdapters` used only paths resolved from the sealed `.runtime` and passed the complete approval, isolated-process, numerical-validation, CAS readback, artifact, and execution-receipt path:

- OpenVSP/VSPAERO: angle of attack 0/2/4 degrees produced CL `0 / 0.156999 / 0.313729` and CD `0.00519126 / 0.00660033 / 0.0108241`.
- OpenVSP model modification: sweep changed from 10 to 15 degrees and the output model differed from its source.
- Gmsh: 183 nodes, 364 elements, and coherence validation passed.
- XFOIL: CL `0 / 0.2157 / 0.4303` and CD `0.00543 / 0.00583 / 0.00732`.
- SU2 8.5.0 win64-omp: Mach 0.8, AoA 1.25 degrees, and a 0.02 mesh converged at iteration 121; RMS density decreased from `-0.8470948844` to `-8.000391599`, CL was `0.3518913725`, and CD was `0.0154278913`.

The final E2E log is `build/tool-smoke/real-engineering-adapters-e2e-sealed-final.log`, SHA-256 `e16d261c99f5ae345251dc1e0dadcaf586f041a135e59faa643d123b3bcab5c8`. The full dependency policy, npm audit/build, Go module verification, `go test ./...`, and `go vet ./...` also passed after these changes.

The built `aetherops.exe engineering-mcp` was separately exercised over real stdio JSONL. It initialized as `aetherops-engineering` with MCP protocol `2025-06-18`, returned exactly the six intended typed tools, emitted no stderr, and exited cleanly at EOF.

The deterministic Codex and embedding protocol fixtures validate state transitions, schema failures, interruption behavior, artifact ordering, and RAG persistence only. They are not release-quality research results.

## Completion boundary

| Area | Current evidence | Release status |
| --- | --- | --- |
| Core, storage, recovery, scheduler, browser policy, engineering adapters, ontology/RDF/SPARQL, graph UI, and local packaged recovery | Bound to candidate `a8514a…`, with typed append-only receipts for all five local gates and real Windows UI/SPARQL inspection | 5/13 admission gates passed; external gates pending |
| Package trust proof and package construction | Build-time embedded trust diagnostic, exact packaging-input comparison, fail-closed parser, environment-cleared portable launch, reproducible SBOM verification, notice/license policy, and final two-artifact-only hash manifest are implemented and have targeted local tests; no package or hash has been regenerated after these changes | Pending package regeneration |
| ChatGPT OAuth, exact production model availability, OpenAI embeddings, and the full live research/browser/MCP path | Requires the user's real credentials and live services; fixtures are deliberately insufficient | Pending external evidence |
| Stable update channel | Candidate verification, quarantine, and restart activation are implemented; a real private feed and signing trust root are not present in the repository | Pending deployment evidence |
| Research quality gate | The immutable 12-case dataset, live product runner, durable ambiguity-safe journal, protected session descriptor, offline verifier, and typed evidence admission are ready; no eligible live runner receipt exists yet | Pending 12/12 live runs |
| Windows distribution and hardware matrix | Historical artifacts passed same-host portable and installer smoke tests; the current source has no regenerated artifacts, and clean Windows 11 VMs plus genuinely incompatible SU2 hardware have not been exercised | Pending current artifacts and environment evidence |
| Private repository replacement and alpha tag | Intentionally not executed. The local `main` branch has no commit and no remote configured, preserving the required final gate | Blocked by all rows above |

## Required before release

- Verify the Haswell/AVX2/FMA/BMI/OSXSAVE preflight on compatible and incompatible Windows hardware before allowing SU2 8.5.0 win64-omp. Do not substitute a different SU2 build on failure.
- Complete ChatGPT device-code authentication and prove exact availability of `gpt-5.6-sol/xhigh` and `gpt-5.6-terra/high` through the live App Server.
- Store the user-provided OpenAI Platform key in Windows Credential Manager and run live `text-embedding-3-small` 1536-dimension indexing, hybrid retrieval, and shadow-index replacement.
- Run the live internet WebView2 + Chrome DevTools MCP + internal MCP + CAS readback research path, including real browser interruption/re-observation.
- Configure the external production stable-channel feed and its private signing pipeline, then repeat the live release proof. The product path is wired and locally tested: it checks only while SQLite reports the application idle, verifies an Ed25519-signed feed and per-artifact attestations, enforces SHA-256/npm SRI and bounded extraction, exercises the candidate App Server with the dedicated `CODEX_HOME` and exact `model/list` requirements plus live WebView2 `list_pages`/`take_snapshot`, stages without activation, activates on the next restart, and exposes durable quarantine warnings. This source tree intentionally ships no fabricated feed URL or trust key, so an ordinary unconfigured development build reports the updater as disabled. A release build must receive the three private inputs at link time and pass the executable's redacted embedded-trust diagnostic before staging.
- Pass all 12 versioned research evaluations (6 general and 6 engineering/academic) through the same automatic quality gate.
- Regenerate the executable, portable ZIP, installer, SBOM/notices, and `dist/SHA256SUMS.txt` from the final source with the real private trust inputs. Record new hashes only after the complete package gate passes.
- Run that regenerated installer and portable package through installation, solver execution, restart, runtime-update quarantine, and removal scenarios on clean Windows 11 VMs.
- Only after every remaining release gate passes, perform the explicitly gated private GitHub repository replacement and `v0.1.0-alpha.1` tag.

No remote repository mutation is permitted while any required item above is pending.
